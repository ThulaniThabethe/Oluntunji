@model List<WebApplication1.Models.Review>

@{
    ViewBag.Title = "My Reviews";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-12">
            <h2>@ViewBag.Title</h2>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")">Home</a></li>
                    <li class="breadcrumb-item"><a href="@Url.Action("Index", "Profile")">Profile</a></li>
                    <li class="breadcrumb-item active">My Reviews</li>
                </ol>
            </nav>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="row">
        <div class="col-md-12">
            @if (Model == null || !Model.Any())
            {
                <div class="alert alert-info">
                    <h5>No Reviews Found</h5>
                    <p>You haven't written any reviews yet. Start exploring books and share your thoughts!</p>
                    <a href="@Url.Action("Browse", "Books")" class="btn btn-primary">Browse Books</a>
                </div>
            }
            else
            {
                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Your Reviews (@Model.Count)</h5>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table id="reviewsTable" class="table table-striped table-hover">
                                <thead>
                                    <tr>
                                        <th>Book</th>
                                        <th>Rating</th>
                                        <th>Review</th>
                                        <th>Date</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var review in Model)
                                    {
                                        <tr>
                                            <td>
                                                <div class="d-flex align-items-center">
                                                    @if (review.Book != null && !string.IsNullOrEmpty(review.Book.CoverImageUrl))
                                                    {
                                                        <img src="@review.Book.CoverImageUrl" alt="@review.Book.Title" class="img-thumbnail me-2" style="width: 50px; height: 70px; object-fit: cover;">
                                                    }
                                                    else
                                                    {
                                                        <div class="bg-secondary text-white d-flex align-items-center justify-content-center me-2" style="width: 50px; height: 70px;">
                                                            <small>No Image</small>
                                                        </div>
                                                    }
                                                    <div>
                                                        <strong>@(review.Book != null ? review.Book.Title : "Unknown Book")</strong>
                                                        <br />
                                                        <small class="text-muted">by @(review.Book != null ? review.Book.Author : "Unknown Author")</small>
                                                    </div>
                                                </div>
                                            </td>
                                            <td>
                                                <div class="rating-stars">
                                                    @for (int i = 1; i <= 5; i++)
                                                    {
                                                        if (i <= review.Rating)
                                                        {
                                                            <i class="fas fa-star text-warning"></i>
                                                        }
                                                        else
                                                        {
                                                            <i class="far fa-star text-warning"></i>
                                                        }
                                                    }
                                                    <br />
                                                    <small class="text-muted">@review.Rating/5</small>
                                                </div>
                                            </td>
                                            <td>
                                                <div class="review-text" style="max-width: 300px;">
                                                    @if (!string.IsNullOrEmpty(review.ReviewText))
                                                    {
                                                        @Html.Raw(review.ReviewText.Length > 100 ? review.ReviewText.Substring(0, 100) + "..." : review.ReviewText)
                                                    }
                                                    else
                                                    {
                                                        <span class="text-muted">No written review</span>
                                                    }
                                                </div>
                                            </td>
                                            <td>
                                                <small>@review.ReviewDate.ToString("MMM dd, yyyy")</small>
                                            </td>
                                            <td>
                                                <div class="btn-group btn-group-sm">
                                                    <a href="@Url.Action("Details", "Book", new { id = review.BookId })" class="btn btn-outline-primary" title="View Book">
                                                        <i class="fas fa-eye"></i>
                                                    </a>
                                                    @if (User.IsInRole("Admin"))
                                                    {
                                                        <button type="button" class="btn btn-outline-danger" onclick="deleteReview(@review.ReviewId)" title="Delete Review">
                                                            <i class="fas fa-trash"></i>
                                                        </button>
                                                    }
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            $('#reviewsTable').DataTable({
                "order": [[3, "desc"]], // Sort by date descending
                "pageLength": 10,
                "responsive": true,
                "language": {
                    "search": "Search reviews:",
                    "lengthMenu": "Show _MENU_ reviews per page",
                    "info": "Showing _START_ to _END_ of _TOTAL_ reviews",
                    "paginate": {
                        "first": "First",
                        "last": "Last",
                        "next": "Next",
                        "previous": "Previous"
                    }
                }
            });
        });

        function deleteReview(reviewId) {
            if (confirm('Are you sure you want to delete this review?')) {
                $.ajax({
                    url: '@Url.Action("DeleteReview", "Book")',
                    type: 'POST',
                    data: { reviewId: reviewId },
                    success: function(result) {
                        if (result.success) {
                            location.reload();
                        } else {
                            alert('Error: ' + result.message);
                        }
                    },
                    error: function() {
                        alert('An error occurred while deleting the review.');
                    }
                });
            }
        }
    </script>
}